# helpers/

Shared pure utility functions (no React, no server coupling). Convention from
phase2/2-client-structure. Empty since the Redux reducer helpers were removed
in the React Router migration — add new helpers here rather than inside
feature folders when they are reused across features.
